/**
 * RequestComponent
 *
 * This component serves as the main container for the appointment request process.
 * It acts as a shell component that loads the appropriate child views based on routing.
 */

import { Component } from '@angular/core';

@Component({
    selector: 'app-request',
    standalone: false,
    templateUrl: './request.component.html',
    styleUrl: './request.component.scss',
})
export class RequestComponent { }
